import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.assesment.angular',
  appName: 'angular-assessment',
  webDir: 'dist/assessment',
  bundledWebRuntime: true
};

export default config;
